spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/cruisin
    username: sa
    password:
    driver-class-name: org.h2.Driver

    jpa:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

#logging:
#  level:
#    root: debug
logging:
  level:
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
